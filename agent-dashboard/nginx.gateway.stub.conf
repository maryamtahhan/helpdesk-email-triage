    # Replaced at container start by bootstrap_welcome.py.
    # Health-only probe; the gateway API is never proxied through the dashboard.
    location = /api/gateway/health {
        return 503;
    }
