apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gateway-data
  labels:
    app.kubernetes.io/part-of: helpdesk-email-triage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
