apiVersion: v1
kind: Secret
metadata:
  name: helpdesk-secrets
  labels:
    app.kubernetes.io/part-of: helpdesk-email-triage
type: Opaque
stringData:
  # Replace before any real deployment. Used by the gateway vault API and dashboard.
  VAULT_SECRET: helpdesk-demo-secret
  # When set, POST /ingest and /ingest/raw require header X-Ingest-Key.
  INGEST_API_KEY: ""
