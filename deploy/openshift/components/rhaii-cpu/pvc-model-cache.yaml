apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rhaii-model-cache
  labels:
    app.kubernetes.io/name: rhaii-cpu
    app.kubernetes.io/component: inference
    app.kubernetes.io/part-of: helpdesk-email-triage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
