apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Deploy into an existing OpenShift project (do not create helpdesk-email-triage namespace).
namespace: mtahhan-quickstart

resources:
  - ../mock-demo

patches:
  - path: patch-drop-namespace.yaml
